server:
  port: 8084
spring:
  application:
    name: CONFIG-SERVER

  cloud:
    config:
      server:
        git:
          uri: https://github.com/Abhishek009-1/microservice-config
          clone-on-start: true